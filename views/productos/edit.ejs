<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Producto - Aká Mercado</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <div class="container">
            <h1 class="title">Editar Producto</h1>
        </div>
    </header>

    <main>
        <div class="container">
            <form action="/productos/edit/<%= producto._id %>" method="POST" enctype="multipart/form-data" class="form-container">
                <div class="form-group">
                    <label for="name" class="form-label">Nombre del producto</label>
                    <input type="text" name="name" id="name" value="<%= producto.name %>" required class="form-input">
                </div>

                <div class="form-group">
                    <label for="price" class="form-label">Precio</label>
                    <input type="number" name="price" id="price" value="<%= producto.price %>" required class="form-input">
                </div>

                <div class="form-group">
                    <label for="quantity" class="form-label">Cantidad</label>
                    <input type="number" name="quantity" id="quantity" value="<%= producto.quantity %>" required class="form-input">
                </div>

                <div class="form-group">
                    <label for="category" class="form-label">Categoría</label>
                    <select name="category" id="category" required class="form-input">
                        <option value="<%= producto.category %>" selected><%= producto.category %></option>
                        <option value="Botilleria">Botillería</option>
                        <option value="Cotiseria">Cotisería</option>
                        <option value="Carniceria">Carnicería</option>
                        <option value="Aseo">Aseo</option>
                        <option value="Bebidas">Bebidas</option>
                        <option value="Abarrotes">Abarrotes</option>
                        <option value="Tabaqueria">Tabaquería</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="image" class="form-label">Imagen</label>
                    <input type="file" name="image" id="image" accept="image/*" class="form-input">
                </div>

                <button type="submit" class="btn-submit">Actualizar Producto</button>
            </form>
        </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>